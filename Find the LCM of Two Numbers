def lcm(a, b):
    greater = max(a, b)
    while True:
        if greater % a == 0 and greater % b == 0:
            return greater
        greater += 1

num1 = 12
num2 = 15
print("LCM of", num1, "and", num2, "is", lcm(num1, num2))
