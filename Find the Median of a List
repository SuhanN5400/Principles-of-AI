my_list = [1, 3, 3, 6, 7, 8, 9]
sorted_list = sorted(my_list)
n = len(sorted_list)
median = (sorted_list[n // 2] if n % 2 != 0 else (sorted_list[n // 2 - 1] + sorted_list[n // 2]) / 2)
print("Median of the list is", median)
